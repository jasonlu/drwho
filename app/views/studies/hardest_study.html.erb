<h1>易錯排行榜</h1>
<h2>全部課程易錯排行榜</h2>

<table class="table table-striped">
  <tr class="row">
    <th>易錯單字、片語、或例句</th>
    <th class="span2">測驗錯誤次數</th>
    <th class="span2">課程編號</th>
  </tr>
  <% @records.each do |record| %>
  <% study_id = record.study.uuid %>
  <tr class="row">
    <td class=""><%= record.course_item.english %></td>
    <td class="span2"><%= record.cnt %></td>
    <td class="span2"><%= link_to record.course.serial, study_hardests_path(study_id, record.course_id) %></td>
  </tr>
  <% end %>
</table>