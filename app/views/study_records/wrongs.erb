<h3>易錯排行榜</h3>
<h4>全部課程易錯排行榜</h4>
<% if @records.length == 0 %>
<p class="alert alert-info">您沒有任何錯誤，真是太棒了！</p>
<% else %>
<table class="table table-striped">
  <tr class="row">
    <th>易錯單字、片語、或例句</th>
    <th class="span2">測驗錯誤次數</th>
    <th class="span2">課程編號</th>
  </tr>
  <% @records.each do |record| %>
  <tr class="row">
    <td class=""><%= record.course_item.english %></td>
    <td class="span2"><%= record.cnt %></td>
    <td class="span2"><%= link_to record.course.serial, wrongs_in_course_path(record.study.uuid) %></td>
  </tr>
  <% end %>
</table>
<% end %>